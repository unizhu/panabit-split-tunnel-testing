# Split Tunnel Test Addon Configuration

# Test endpoints configuration
# These endpoints will be tested to verify routing policies

# Primary test endpoints (ip.skk.moe)
PRIMARY_ENDPOINTS="0.ip.skk.moe 1.ip.skk.moe 2.ip.skk.moe 3.ip.skk.moe 4.ip.skk.moe 5.ip.skk.moe 6.ip.skk.moe 7.ip.skk.moe 8.ip.skk.moe 9.ip.skk.moe"

# Alternative test endpoints
ALT_ENDPOINTS="ip.sb ipinfo.io ip-api.com"

# Test domains for DNS resolution testing
TEST_DOMAINS="chatgpt.com claude.ai discord.com testingcf.jsdelivr.net api.openai.com"

# Timeout settings (in seconds)
TEST_TIMEOUT=5
CONNECT_TIMEOUT=3

# Display settings
MAX_RESULTS_DISPLAY=20
AUTO_REFRESH_ENABLED=0
AUTO_REFRESH_INTERVAL=30

# Logging
LOG_ENABLED=1
LOG_FILE=/usr/panabit/data/split_tunnel/test.log
LOG_MAX_SIZE=1048576

# Advanced settings
PARALLEL_TESTS=1
RETRY_COUNT=2
RETRY_DELAY=1
